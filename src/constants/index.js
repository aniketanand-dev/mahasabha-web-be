const STATUS_CODES = require("./status-codes");
const MESSAGES = require("./messages");
const ROLES = require("./roles");
const JWT = require("./jwt");
const COLLECTION_NAMES = require("./collection-names");
const STATIC_VALUES = require("./static-values");
const UPLOAD = require("./upload");

module.exports = {
  STATUS_CODES,
  MESSAGES,
  ROLES,
  JWT,
  COLLECTION_NAMES,
  STATIC_VALUES,
  UPLOAD
};
